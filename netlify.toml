[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Set VITE_API_BASE in the Netlify UI to point to your backend, e.g. https://api.example.com
  NODE_VERSION = "18"
